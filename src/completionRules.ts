export const contextCandidates: Readonly<Record<string, readonly string[]>> = {
  'declaration.root': [
    'package name:',
    'module name:',
    'scope name:',
  ],
  'declaration.package': [
    'package name:',
    'module name:',
    'scope name:',
  ],
  'declaration.module': [
    'alias name -> type',
    'enum name -> type:',
    'struct name:',
    'template name:',
    'template name():',
    'fn name() -> type:',
    'mut fn name() -> type:',
    'scope name:',
  ],
  'declaration.scope': [
    'alias name -> type',
    'enum name -> type:',
    'struct name:',
    'template name:',
    'template name():',
    'fn name() -> type:',
    'mut fn name() -> type:',
    'scope name:',
  ],
  'fn.body': [
    'let name -> type = value',
    'return ',
    'use c.expr("")',
  ],
  'struct.body': [
    'field name -> type',
    'mut field name -> type',
    'use c.ptr(value)',
    'fn name() -> type:',
    'mut fn name() -> type:',
  ],
  'template.body': [
    'name',
    'name -> any',
    'name -> template',
    '... -> values',
    'field name -> type',
    'mut field name -> type',
    'use c.ptr(value)',
  ],
  'attribute': [
    '@pub(header)',
    '@pub(source)',
    '@pub(all)',
    '@package(inline)',
    '@include("header.h")',
    '@enum(static)',
    '@enum(define)',
    '@enum(extern)',
    '@fn(static)',
    '@fn(extern)',
    '@fn(inline)',
    '@fn(static, inline)',
    '@template(inline)',
    '@template(inline, define)',
    '@template(mutable)',
  ],
};
